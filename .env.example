# Gemini Developer API (https://aistudio.google.com/apikey — no GCP project needed)
GOOGLE_GENAI_USE_VERTEXAI=false
GOOGLE_API_KEY=

# Model overrides (defaults live in shared/config.py)
CONCIERGE_MODEL=gemini-2.5-pro
DISPUTE_TRIAGE_MODEL=gemini-2.5-pro
PRICING_MODEL=gemini-2.5-flash
ONBOARDING_MODEL=gemini-2.5-flash
LISTING_INTAKE_MODEL=gemini-2.5-flash

# Postgres (Supabase) — SESSION-mode pooler (port 5432 on the pooler host;
# username must be <role>.<project-ref>). Works over IPv4. Do NOT use the
# transaction-mode port 6543 (breaks asyncpg prepared statements); the
# direct db.<ref>.supabase.co host is IPv6-only on most plans.
# App runtime uses the surplusas_agents_app role; provisioning scripts need
# the postgres owner DSN.
DATABASE_URL=postgresql://surplusas_agents_app.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres

# Webhooks (any long random string; Fly secret in prod)
WEBHOOK_SIGNING_KEY=

# Local dev
LOG_LEVEL=INFO
PORT=8080
# Mounts the unauthenticated /demo/v1 shim + static demo UI. NEVER in prod.
DEMO_MODE=true

# Optional: export OTel spans to any OTLP endpoint (unset = spans stay in-process)
# OTEL_EXPORTER_OTLP_ENDPOINT=
